const express = require("express")
const router = express.Router()
const userCtl = require('../controllers/user.js')

// router.get('/', userCtl.getAllUsers)
// router.get('/:user', userCtl.getUserById)

module.exports = router